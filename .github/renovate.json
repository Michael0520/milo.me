{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommitTypeAll(chore)",
    "group:monorepos"
  ],
  "labels": ["dependencies"],
  "minimumReleaseAge": "3 days",
  "prConcurrentLimit": 5,
  "prHourlyLimit": 2,
  "skipArtifactsUpdate": true,
  "gitIgnoredAuthors": ["307198022+milo-renovate-guard-ml[bot]@users.noreply.github.com"],
  "github-actions": {
    "description": "Dependabot owns GitHub Actions updates (.github/dependabot.yml) — avoid duplicate PRs.",
    "enabled": false
  },
  "packageRules": [
    {
      "description": "Group non-major npm/catalog updates into a single PR to cut noise.",
      "matchManagers": ["npm"],
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "npm minor & patch"
    },
    {
      "description": "Held majors — kept back on purpose (see project memory / CLAUDE.md).",
      "matchPackageNames": ["zod", "@hookform/resolvers", "@types/node", "puppeteer", "pnpm"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "sharp — 0.x package, so a minor bump (0.34 -> 0.35) is a breaking change that Renovate mislabels as 'minor' and would otherwise slip past the held-major lock. Next.js's image pipeline depends on it. Lock minor AND major; only patch within the current 0.34.x is allowed.",
      "matchPackageNames": ["sharp"],
      "matchUpdateTypes": ["major", "minor"],
      "enabled": false
    },
    {
      "description": "TypeScript held on 6.x — 7.0 is the native (Go) compiler rewrite; the build fails on it today (PR #62). Allow 6.x minor/patch, block the 7.0 major.",
      "matchPackageNames": ["typescript"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Next.js — the 16.1.7 pin was lifted in PR #96 (16.3.0 ships turbopackMemoryEviction on by default, which fixes the Apple Silicon dev-server memory growth of vercel/next.js#92055). 16.x minor/patch now flow through the grouped PR so security fixes are not stranded; the next major stays manual.",
      "matchPackageNames": ["next"],
      "matchUpdateTypes": ["major"],
      "enabled": false
    },
    {
      "description": "Slidev held at 52.6.0 — 52.15+ ships Vite 8, which breaks the shared-asset symlinks in apps/slides.",
      "matchPackageNames": ["@slidev/cli", "@slidev/client", "@slidev/theme-default"],
      "enabled": false
    },
    {
      "description": "vite-plus toolchain — bumped deliberately, not automatically.",
      "matchPackageNames": [
        "vite-plus",
        "@voidzero-dev/vite-plus-core",
        "@voidzero-dev/vite-plus-test"
      ],
      "enabled": false
    }
  ]
}
