{
  "name": "milo-vite-plus",
  "version": "0.12.1",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "dev": "vp run website#dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@fission-ai/openspec": "catalog:script",
    "vite-plus": "catalog:build"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@10.34.4"
}
